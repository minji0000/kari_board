<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="mapper.UserMapper">    <insert id="register" parameterType="com.kari.kariboard.entity.UserEntity">        INSERT INTO user(username, password, name, birth, phone, nickname, email)        VALUES (#{username}, #{password}, #{name}, #{birth}, #{phone}, #{nickname}, #{email})    </insert>    <select id = "auth" resultType="UserDTO" parameterType="com.kari.kariboard.entity.UserEntity">        SELECT * FROM user WHERE username = #{username}                             AND password = #{password}    </select>    <select id = "selectOne" parameterType = "int" resultType = "com.kari.kariboard.entity.UserEntity">        SELECT * FROM user        WHERE id = #{id}    </select></mapper>